# 1
square = lambda x: x * x
print(square(5))

# 2
add = lambda a, b: a + b
print(add(2, 3))

# 3
is_even = lambda x: x % 2 == 0

# 4
max_two = lambda a, b: a if a > b else b

# 5
length = lambda s: len(s)